{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "category": {
      "type": "string",
      "enum": ["Billing", "Technical", "Sales", "General"]
    },
    "priority": {
      "type": "string",
      "enum": ["Low", "Medium", "High", "Urgent"]
    },
    "sentiment": {
      "type": "string",
      "enum": ["Positive", "Neutral", "Frustrated", "Angry"]
    },
    "department": {
      "type": "string",
      "enum": ["Finance", "Engineering", "Sales", "Support"]
    },
    "summary": {
      "type": "string",
      "description": "A brief summary of the customer issue."
    },
    "suggested_response": {
      "type": "string",
      "description": "An automated reply drafted for the customer."
    }
  },
  "required": [
    "category",
    "priority",
    "sentiment",
    "department",
    "summary",
    "suggested_response"
  ]
}